import styled from "styled-components";
import HomeContainer from "../components/HomeContainer";

const Container = styled.div``;

const Practice = () => {
  return (
    <Container>
      <HomeContainer practice rank={false} />
    </Container>
  );
};

export default Practice;
